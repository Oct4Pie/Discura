{
    "name": "discura-monorepo",
    "version": "0.1.0",
    "private": true,
    "description": "Discura - Discord Bot Management Platform",
    "scripts": {
        "check": "cd common && npx tsc --noEmit; cd ../backend && npx tsc --noEmit; cd ../frontend && npx tsc --noEmit",
        "init": "node init.js",
        "dev": "npm run init && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "cd backend && npm run dev",
        "dev:frontend": "cd frontend && npm run dev",
        "build:all": "npm run build:common && npm run build:backend && npm run build:frontend",
        "build": "./generate-api-types.sh && npm run build:all",
        "install:all": "npm install && npm run install:workspaces",
        "install:workspaces": "cd common && npm install && cd ../backend && npm install && cd ../frontend && npm install",
        "setup": "npm run install:all && npm run init"
    },
    "devDependencies": {
        "@types/module-alias": "^2.0.4",
        "concurrently": "^8.2.0",
        "madge": "^8.0.0",
        "rimraf": "^5.0.0",
        "tsconfig-paths": "^4.2.0",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "workspaces": [
        "common",
        "backend",
        "frontend"
    ],
    "dependencies": {
        "mkdirp": "^3.0.1"
    }
}
